# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## Project Overview

wedding-texter is a self-hosted, lower-cost alternative to paid wedding guest text-reminder services (e.g. WedTexts, Text My Wedding). The goal is to let a couple send SMS reminders to their guest list without a premium subscription.

## Current State

The repository is in its earliest stage — only a README exists. No source code, dependencies, or configuration have been added yet. Update this file as the project takes shape.

## What to add next

Once the tech stack and project scaffolding are chosen, add the following sections to this file:

- **Commands** — how to install deps, build, lint, run tests, and run a single test file/case.
- **Architecture** — the high-level structure: how the app is layered, where the SMS-sending logic lives, how guest data is stored/loaded, and how the pieces connect.
- **Key decisions** — any non-obvious choices (SMS provider/API, data store, framework) and the reasoning behind them, so future work doesn't relitigate them.
